import { ICloudBaseConfig } from '../types';
export interface ResolveOptions {
    searchFrom?: string;
    configPath?: string;
}
export declare function resolveCloudBaseConfig(options: ResolveOptions): Promise<ICloudBaseConfig>;
export declare function getEnvId(commandOptions: any): Promise<string>;
export interface ICloudBaseOptions {
    cwd: string;
    cover?: boolean;
    configPath?: string;
}
export declare const renderConfig: (template: string, view: Record<string, any>) => any;
export declare class ConfigParser {
    static instance: ConfigParser;
    static get(options: ICloudBaseOptions, key?: string, defaultValue?: any): Promise<any>;
    static update(options: ICloudBaseOptions, key?: string, value?: any): Promise<void>;
    private cwd;
    private configPath;
    private cover;
    constructor(options: ICloudBaseOptions);
    get<T>(key?: string, defaultValue?: T): Promise<any>;
    update(key: string | Record<string, any>, value?: any): Promise<void>;
    resolveConfig(searchFrom?: string, configPath?: string): Promise<ICloudBaseConfig>;
    writeConfig(config: ICloudBaseConfig, cover?: boolean): Promise<void>;
    options(options: ICloudBaseOptions): this;
}
